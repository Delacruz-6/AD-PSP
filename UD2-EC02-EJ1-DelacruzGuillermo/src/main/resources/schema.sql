CREATE TABLE country (
    id   INTEGER      NOT NULL AUTO_INCREMENT,
    name VARCHAR(128) NOT NULL,
    PRIMARY KEY (id)
);
insert into country values (2, 'Francia')
--Spring boot ejecuta schema o data directamente
